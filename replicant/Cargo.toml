[package]
name = "replicant"
version = "0.1.2"
edition = "2021"
description = "Offline-first document synchronization library"

[dependencies]
replicant-core = { path = "../replicant-core" }
replicant-client = { path = "../replicant-client" }
replicant-server = { path = "../replicant-server" }
